# 水利工程技术语音问答助手 — 使用说明

## 1. 项目概述

基于 DeepSeek 大语言模型 + 百度智能云 ASR/TTS 的水利工程技术语音问答助手。支持文本问答（SSE 流式输出）、语音识别、语音合成三大核心功能，覆盖防洪、灌溉、水电、航运、基础处理等水利专业领域。

### 技术栈

| 层 | 技术 |
|---|---|
| Web 框架 | Flask 2.3+ (Blueprint RESTful API) |
| 大模型 | DeepSeek Chat（主）/ 文心一言·千帆 / 通义千问（回落） |
| 语音识别 | 百度智能云短语音识别 (OAuth 2.0) |
| 语音合成 | 百度智能云语音合成 (PCM 16K) |
| 数据库 | SQLite + SQLAlchemy ORM |
| 向量检索 | Milvus (可选) |
| 前端 | 单页 HTML + SSE 流式渲染 + Web Audio API + KaTeX |

### 核心模块

```
VCL → ASR → LLM → TTS → VRL
语音采集  语音识别  核心交互  语音合成  语音响应
(浏览器)  (百度API)  (DeepSeek)  (百度API)  (Web Audio)
```

---

## 2. 环境准备

### 2.1 依赖安装

```bash
cd water_conservancy_assistant
pip install -r requirements.txt
```

### 2.2 配置文件 `.env`

在项目根目录创建 `.env` 文件（可参考下方模板）：

```ini
# 调试模式（开发环境设为 true）
WCA_DEBUG=true

# Flask 密钥
WCA_SECRET_KEY=your-secret-key

# ---- DeepSeek API（必填） ----
WCA_LLM_API_KEY=sk-your-deepseek-key
WCA_LLM_API_BASE=https://api.deepseek.com
WCA_LLM_MODEL=deepseek-chat

# ---- 百度 ASR（语音识别，必填） ----
WCA_ASR_API_KEY=your-baidu-api-key
WCA_ASR_SECRET_KEY=your-baidu-secret-key
WCA_ASR_APP_ID=your-baidu-app-id

# ---- 百度 TTS（语音合成，必填） ----
WCA_TTS_API_KEY=your-baidu-api-key
WCA_TTS_SECRET_KEY=your-baidu-secret-key
WCA_TTS_APP_ID=your-baidu-app-id
```

> **获取 API 密钥**：
> - DeepSeek: <https://platform.deepseek.com/api_keys>
> - 百度智能云 ASR/TTS: <https://console.bce.baidu.com/ai/#/ai/speech/overview/index>
>
> **多模型支持**：除 DeepSeek 外，项目依赖 `qianfan`（百度千帆/文心）和 `dashscope`（阿里通义）SDK，可通过 `WCA_LLM_PROVIDER` 切换主模型。

### 2.3 配置项完整列表

| 环境变量 | 默认值 | 说明 |
|---|---|---|
| `WCA_DEBUG` | false | 调试模式，开启后自动重载 |
| `WCA_SECRET_KEY` | — | Flask 应用密钥 |
| `WCA_JWT_SECRET` | — | JWT 签名密钥（默认同 SECRET_KEY） |
| `WCA_JWT_EXPIRE_HOURS` | 24 | JWT 过期时间（小时） |
| `WCA_LLM_PROVIDER` | deepseek | LLM 提供商：deepseek / qianfan / dashscope |
| `WCA_LLM_API_KEY` | — | LLM API Key |
| `WCA_LLM_API_BASE` | api.deepseek.com | LLM API 地址 |
| `WCA_LLM_MODEL` | deepseek-chat | 模型名称 |
| `WCA_LLM_APP_ID` | — | LLM 应用 ID（千帆等需要） |
| `WCA_ASR_PROVIDER` | baidu | ASR 提供商 |
| `WCA_ASR_API_KEY` | — | 百度 ASR API Key |
| `WCA_ASR_SECRET_KEY` | — | 百度 ASR Secret Key |
| `WCA_ASR_APP_ID` | — | 百度 ASR App ID |
| `WCA_TTS_PROVIDER` | baidu | TTS 提供商 |
| `WCA_TTS_API_KEY` | — | 百度 TTS API Key |
| `WCA_TTS_SECRET_KEY` | — | 百度 TTS Secret Key |
| `WCA_TTS_APP_ID` | — | 百度 TTS App ID |
| `WCA_DATABASE_PATH` | data/water_knowledge.db | SQLite 数据库路径 |
| `WCA_SESSION_TTL_MINUTES` | 30 | 会话过期时间 |
| `WCA_MAX_HISTORY_ROUNDS` | 10 | 最大对话轮数 |
| `WCA_RAG_TOP_K` | 5 | RAG 检索返回条数 |
| `WCA_SIMILARITY_THRESHOLD` | 0.75 | RAG 相似度阈值 |
| `WCA_EMBEDDING_MODEL` | text-embedding-v1 | 向量嵌入模型 |
| `WCA_DEFAULT_SAMPLE_RATE` | 16000 | 默认音频采样率 |
| `WCA_DEFAULT_BIT_DEPTH` | 16 | 默认音频位深 |
| `WCA_RATE_LIMIT_PER_MINUTE` | 60 | 每分钟请求限额 |
| `WCA_MILVUS_HOST` | — | Milvus 向量库地址（可选） |
| `WCA_MILVUS_PORT` | 19530 | Milvus 端口 |
| `WCA_REDIS_URL` | — | Redis 地址（可选，用于分布式会话） |
| `WCA_LOG_DIR` | data/logs | 日志文件目录 |
| `WCA_LOG_LEVEL` | INFO | 日志级别 |

---

## 3. 启动运行

### 3.1 开发模式

```bash
cd water_conservancy_assistant
python main.py
```

服务默认监听 `http://0.0.0.0:5000`，浏览器打开 <http://localhost:5000> 即可看到测试页面。

> 端口可通过 `WCA_PORT` 环境变量自定义。

### 3.2 生产模式

```bash
# 关闭调试
export WCA_DEBUG=false

# Gunicorn（Linux/Mac）
gunicorn -w 4 -b 0.0.0.0:5000 main:app

# 或 Waitress（Windows）
pip install waitress
waitress-serve --port=5000 main:app
```

### 3.3 Docker

```bash
docker build -t water-assistant .
docker run -p 5000:5000 --env-file .env water-assistant
```

---

## 4. 初始化数据库

```bash
# 建表 + 验证
python scripts/init_db.py

# 导入种子数据（术语库、示例配置等）
python scripts/seed_data.py
```

成功后会在 `data/water_knowledge.db` 创建 11 张表（user, session, message, config, log, knowledge_base, terminology, api_log, model_evaluation, user_feedback, system_metric）。

---

## 5. API 接口

Base URL: `http://localhost:5000/api/v1`

### 5.1 健康检查

**`GET /health`**

```json
{"status":"ok","service":"water-conservancy-assistant","version":"2.0.0"}
```

### 5.2 文本问答（非流式）

**`POST /chat`**

请求：
```json
{"question": "什么是帷幕灌浆？"}
```

响应：
```json
{
  "answer": "帷幕灌浆是指在坝基或岸坡中钻孔...",
  "sources": ["SL 570-2013"],
  "confidence": 0.92
}
```

### 5.3 文本问答（SSE 流式）🆕

**`POST /chat/stream`**

请求：
```json
{
  "question": "水利工程分为哪几个等级？",
  "history": [],
  "temperature": 0.3,
  "top_p": 0.85
}
```

响应（Server-Sent Events）：

```
data: {"token": "水利"}

data: {"token": "工程"}

data: {"token": "分为"}

...

data: {"done": true, "answer": "水利工程分为五个等级...", "interrupted": false}
```

- 每个 `token` 事件包含一个文本片段，前端实时追加渲染。
- 最后一个事件 `done: true` 包含完整答案。
- **客户端断开连接即视为打断**，服务端自动中止 LLM 生成，返回 `interrupted: true`。
- 可通过 `temperature` 和 `top_p` 参数调整生成策略。

### 5.4 语音识别

**`POST /voice/recognize`**

请求（base64 编码的 PCM 音频）：
```json
{
  "audio_data": "<base64-pcm>",
  "sample_rate": 16000,
  "bit_depth": 16,
  "channels": 1
}
```

响应：
```json
{
  "status": "success",
  "text": "什么是帷幕灌浆",
  "raw_text": "什么是帷幕灌浆",
  "confidence": 0.95
}
```

### 5.5 语音合成

**`POST /voice/synthesize`**

请求：
```json
{"text": "水利工程的主要类型包括防洪工程。"}
```

响应：
```json
{
  "status": "success",
  "audio_data": "<base64-wav>",
  "duration": 2.5,
  "sample_rate": 16000
}
```

> 文本最长 500 字，超过返回 `T002` 错误。返回的音频为 WAV 格式（服务端将百度 PCM 16K 封装为 WAV）。

### 5.6 完整语音问答流程

**`POST /voice/process`**

一步完成 VCL → ASR → LLM → TTS → VRL 全链路。

请求：
```json
{
  "audio_data": "<base64-pcm>",
  "session_id": "可选",
  "user_id": "anonymous"
}
```

响应：
```json
{
  "status": "success",
  "session_id": "sess-xxx",
  "question": "什么是帷幕灌浆",
  "answer": "帷幕灌浆是指...",
  "asr_confidence": 0.95,
  "domain": "基础处理",
  "sources": ["SL 570-2013"],
  "term_match_rate": 0.85,
  "hallucination_score": 0.05,
  "audio_data": "<base64-wav>",
  "audio_duration": 3.1,
  "timeline": {
    "capture": "done",
    "asr": {"text": "什么是帷幕灌浆", "confidence": 0.95},
    "llm": {"domain": "基础处理", "confidence": 0.9},
    "tts": {"duration": 3.1},
    "vrl": {"state": "done", "volume": 70}
  }
}
```

> 新增字段：`sources`（引用规范）、`term_match_rate`（术语匹配率）、`hallucination_score`（幻觉评分，越低越好）。

### 5.7 语音系统状态

**`GET /voice/status`**

返回 6 个模块的实时状态：
```json
{
  "vcl": {"state": "idle", "status": "idle"},
  "asr": {"provider": "baidu", "status": "idle"},
  "llm": {"status": "idle"},
  "tts": {"status": "idle"},
  "vrl": {"state": "ready", "status": "idle"},
  "session": {"status": "idle"}
}
```

### 5.8 会话管理

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/sessions` | 创建会话 |
| `GET` | `/sessions/<id>` | 获取会话信息 |
| `DELETE` | `/sessions/<id>` | 结束会话 |
| `POST` | `/sessions/<id>/messages` | 发送消息 |
| `GET` | `/sessions/<id>/messages` | 获取消息历史 |

### 5.9 其他接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/knowledge/retrieve` | RAG 知识库检索 |
| `GET` | `/terminology/search?term=帷幕灌浆&fuzzy=true` | 术语库查询（支持模糊匹配） |
| `PUT` | `/model/config` | 调整模型参数（temperature, top_p 等） |

### 5.10 错误码

| 分类 | 示例 | 说明 |
|---|---|---|
| A (Audio) | A001 | 音频格式无效 |
| R (Recognition) | R001 | 语音识别失败 |
| T (TTS) | T001, T002 | 合成失败 / 文本过长（>500字） |
| P (Parameter) | P001 | 参数缺失或无效 |
| S (Session) | S001, S003 | 会话不存在 / 已过期 |
| SYSTEM | SYSTEM001 | 系统内部错误 |

---

## 6. 前端测试页面

访问 <http://localhost:5000>，页面包含以下功能区域：

### 对话面板（左侧）

- **快捷问题按钮**：帷幕灌浆、工程等级、抗裂评估、水锤现象，点击一键提问。
- **SSE 流式渲染** 🆕：AI 回答逐 token 实时显示，支持 Markdown（标题、列表、粗斜体、代码块）和 KaTeX 数学公式渲染。
- **打断功能** 🆕：发送消息后出现 ⏹ 打断按钮，可随时中止 AI 生成。
- **TTS 朗读队列** 🆕：AI 回答按句子自动分段，通过 Web Audio API 依次播放，朗读时也可打断。
- **朗读重播**：每条 AI 回复下方有 🔊 朗读按钮，可手动重播。
- **文本输入框** + 回车发送。

### 语音问答

- 点击 🎤 按钮开始录音，**观察波形条确认麦克风工作**。
- 录音最长 **60 秒**，再次点击 ⏹ 按钮手动停止。
- 识别结果自动作为问题发送给 AI。
- 5 级流水线可视化：采集 → 识别 → 交互 → 合成 → 响应。

### 右侧面板

- **术语库查询**：支持精确/模糊搜索，显示术语定义、分类、来源。
- **模块状态**：6 个核心模块实时健康检测（30 秒轮询）。

### 浏览器兼容性

推荐使用 Chrome 或 Edge 浏览器，以获取完整的 Web Audio API 和 MediaRecorder 支持。

---

## 7. 运行测试

```bash
# 全部测试
pytest tests/ -v

# 仅单元测试
pytest tests/test_unit/ -v

# 仅集成测试
pytest tests/test_integration/ -v

# 带覆盖率报告
pytest tests/ --cov=app --cov-report=html
```

---

## 8. 项目结构

```
water_conservancy_assistant/
├── main.py                  # 应用入口
├── .env                     # 环境变量（API 密钥等）
├── requirements.txt         # Python 依赖
├── config/
│   ├── settings.py          # 全局配置（支持 .env + JSON 双层加载）
│   └── default_config.json  # 默认配置模板
├── app/
│   ├── __init__.py          # Flask App Factory
│   ├── api/v1/              # RESTful API
│   │   ├── chat.py          # 文本问答 + SSE 流式 🆕
│   │   ├── voice.py         # 语音识别/合成/全链路流程
│   │   ├── sessions.py      # 会话管理
│   │   ├── knowledge.py     # 知识库检索
│   │   ├── terminology.py   # 术语库查询
│   │   ├── model.py         # 模型参数调优
│   │   └── health.py        # 健康检查
│   ├── core/                # 核心业务模块
│   │   ├── voice_capture.py      # VCL 语音采集
│   │   ├── speech_recognizer.py  # ASR 语音识别
│   │   ├── core_interaction.py   # LLM 核心交互
│   │   ├── speech_synthesizer.py # TTS 语音合成
│   │   ├── voice_response.py     # VRL 语音响应
│   │   ├── voice_assistant.py    # 编排器（全链路调度）
│   │   ├── session_manager.py    # 会话管理
│   │   ├── config_manager.py     # 配置管理
│   │   ├── error_handler.py      # 错误处理
│   │   ├── fine_tuner.py         # 微调管理
│   │   └── rag/                  # RAG 引擎
│   │       ├── rag_engine.py          # RAG 主引擎
│   │       ├── prompt_manager.py      # 提示词管理
│   │       ├── hallucination_detector.py # 幻觉检测 🆕
│   │       ├── vector_store.py        # 向量存储
│   │       └── embedder.py            # 嵌入模型
│   ├── services/            # 外部 API 封装
│   │   ├── llm_service.py   # DeepSeek LLM（SSE 流式）🆕
│   │   ├── asr_service.py   # 百度 ASR
│   │   ├── tts_service.py   # 百度 TTS（PCM 16K）
│   │   └── circuit_breaker.py # 熔断器
│   ├── models/              # SQLAlchemy 数据模型 (11 表)
│   ├── utils/               # 工具（音频处理、加密、辅助函数）
│   ├── midware/              # 中间件
│   │   ├── auth.py           # JWT 认证
│   │   ├── rate_limiter.py   # 限流
│   │   └── request_logger.py # 请求日志
│   └── static/
│       └── index.html        # 前端 SPA（SSE 流式渲染 + KaTeX）
├── database/                # 数据库连接 + SQL DDL
│   ├── db.py
│   └── schema.py
├── scripts/
│   ├── init_db.py           # 初始化数据库
│   └── seed_data.py         # 导入种子数据
└── tests/
    ├── test_unit/           # 单元测试
    └── test_integration/    # 集成测试
```

---

## 9. 常见问题

**Q: 语音识别一直返回 [R001]？**
1. 观察录音时波形条是否有跳动（确认麦克风输入正常）
2. 使用 Chrome 或 Edge 浏览器（Firefox 的 AudioContext 兼容性有限）
3. 查看服务器控制台的百度 ASR 诊断日志（`err_no` / `err_msg`）
4. 确认百度 API 密钥已正确填入 `.env`，且已开通「短语音识别」服务

**Q: 文本问答无响应？**
1. 确认 DeepSeek API Key 已正确填入 `.env`
2. 检查网络是否能访问 `api.deepseek.com`
3. 查看服务器控制台日志，如有超时错误可检查代理/防火墙
4. 无 API Key 时会返回本地知识库的兜底回答（功能降级但不会报错）

**Q: TTS 语音合成没有声音？**
1. 确认百度 TTS API 密钥已正确填入 `.env`
2. 确认浏览器支持 Web Audio API（Chrome/Edge 推荐）
3. 首次播放需要用户交互（点击按钮）以激活 AudioContext
4. 无 API Key 时会返回静音音频（不报错）

**Q: 如何打断 AI 的回复？**
- 文本问答：点击 ⏹ 打断按钮，或直接发送新问题（新消息自动中断旧流）。
- TTS 朗读：点击 ⏹ 打断按钮，或发送新问题。
- 语音问答：点击 ⏹ 打断按钮。

**Q: 录音最长可以多久？**
60 秒。超过 60 秒会自动停止并提交识别。最少需要 0.5 秒的录音才会被识别。

**Q: Flask 开发服务器警告？**
正常运行会看到 `WARNING: This is a development server.`，仅开发环境显示，不影响使用。生产环境请使用 Gunicorn 或 Waitress 部署。

**Q: 如何切换其他大模型？**
设置环境变量 `WCA_LLM_PROVIDER=qianfan`（百度文心）或 `dashscope`（阿里通义），并填入对应的 API Key。LLM 服务会自动 fallback 到配置的模型。
